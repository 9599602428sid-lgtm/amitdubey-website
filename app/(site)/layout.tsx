import { InvestigationChrome } from "@/components/InvestigationChrome";

export default function SiteLayout({ children }: { children: React.ReactNode }) {
  return <InvestigationChrome>{children}</InvestigationChrome>;
}
